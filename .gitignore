node_modules/
.DS_Store
*.log
.env
.env.*
!.env.example
coverage/
.cache/

# autoresearch run artifacts (keep the harness, ignore its output)
autoresearch/results.tsv
autoresearch/*.log

# Keep dist for npm publishing if desired. Remove this ignore if publishing source-only.
# dist/
